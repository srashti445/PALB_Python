class Solution:
    def find_median(self, arr):
        arr.sort()
        n = len(arr)
        
        # Odd case
        if n % 2 == 1:
            return arr[n // 2]
        
        # Even case
        else:
            return (arr[n // 2] + arr[(n // 2) - 1]) / 2
